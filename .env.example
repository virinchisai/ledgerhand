# Credentials for the MOCK application in apps/legacy_bank. Not real, and not
# connected to anything. They live here rather than in an artifact because an
# artifact is a reviewable document that gets committed.
LEDGERHAND_MCB_OPERATOR=svc.automation
LEDGERHAND_MCB_PASSWORD=Sandbox!Demo1

# Discovery model. Local by default: screen contents never leave the host.
LEDGERHAND_LLM=ollama
LEDGERHAND_MODEL=qwen2.5:7b-instruct
